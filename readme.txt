An express router example that does
  1. Takes in name of the state as params (/states/:name), and sends out their respective State slogan on the license plate
  2. List of states


Use Curl to run the code -
>curl -i http://localhost:3000/states/
>curl -i http://localhost:3000/states/California
